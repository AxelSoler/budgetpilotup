<nav class="bg-white shadow-md mb-6">
  <div class="container mx-auto px-4 py-3 flex justify-between items-center">
    
    <div class="flex items-center space-x-2">
      <%= link_to '💸 BudgetPilotUp', dashboard_path, class: "text-2xl font-extrabold text-blue-700"%>
    </div>

    <div class="hidden md:flex items-center space-x-6">
      <%= link_to 'Dashboard', dashboard_path, 
            class: "px-3 py-1.5 rounded-lg font-semibold transition #{current_page?(dashboard_path) ? 'bg-blue-600 text-white' : 'text-blue-600 hover:text-blue-800'}" %>

      <%= link_to 'Categories', categories_path, 
            class: "px-3 py-1.5 rounded-lg font-semibold transition #{current_page?(categories_path) ? 'bg-blue-600 text-white' : 'text-blue-600 hover:text-blue-800'}" %>

      <%= link_to 'Transactions', transactions_path, 
            class: "px-3 py-1.5 rounded-lg font-semibold transition #{current_page?(transactions_path) ? 'bg-blue-600 text-white' : 'text-blue-600 hover:text-blue-800'}" %>

      <!-- Logout -->
      <%= button_to 'Logout', session_path, method: :delete, 
            class: "ml-4 px-3 py-1.5 bg-red-500 hover:bg-red-600 text-white font-semibold rounded-lg transition cursor-pointer" %>
    </div>

    <div class="md:hidden">
      <button id="mobile-menu-button" class="text-blue-600 hover:text-blue-800 focus:outline-none">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
            d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
    </div>
  </div>

  <!-- Mobile menu -->
  <div id="mobile-menu" class="hidden md:hidden bg-white border-t border-blue-100">
    <div class="px-4 py-2 flex flex-col space-y-2">
      <%= link_to 'Dashboard', dashboard_path, class: "text-blue-600 hover:text-blue-800 font-semibold" %>
      <%= link_to 'Categories', categories_path, class: "text-blue-600 hover:text-blue-800 font-semibold" %>
      <%= link_to 'Transactions', transactions_path, class: "text-blue-600 hover:text-blue-800 font-semibold" %>
      <%= button_to 'Logout', session_path, method: :delete, class: "px-3 py-1.5 bg-red-500 hover:bg-red-600 text-white font-semibold rounded-lg transition" %>
    </div>
  </div>
</nav>

<script>
  // Toggle mobile menu
  const button = document.getElementById("mobile-menu-button");
  const menu = document.getElementById("mobile-menu");

  button.addEventListener("click", () => {
    menu.classList.toggle("hidden");
  });
</script>


