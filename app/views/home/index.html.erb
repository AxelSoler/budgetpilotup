<div class="flex flex-col items-center justify-center h-screen w-full">
  <h1 class="font-bold underline text-4xl">Home#index</h1>
  <p class="text-lg">This is the home page</p>
  <%= link_to 'Login', new_session_path, class: 'mt-4 px-4 py-2 bg-blue-500 text-white rounded' %>
  <%= button_to 'Logout', session_path, method: :delete, class: 'mt-4 px-4 py-2 bg-red-500 text-white rounded cursor-pointer' %>
  <p>Welcome, <%= Current.user.email_address %> to Budgetpilotup!</p>
</div>
